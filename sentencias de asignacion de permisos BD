GRANT SELECT, INSERT, UPDATE, DELETE 
ON matias_fernandez.* 
TO 'usuarios'@'localhost';

GRANT SELECT, INSERT, UPDATE, DELETE,
      CREATE, ALTER, DROP, INDEX,
      TRIGGER, CREATE ROUTINE, ALTER ROUTINE, EXECUTE
ON matias_fernandez.* 
TO 'admin'@'localhost'
WITH GRANT OPTION;

GRANT FILE ON *.*
TO 'admin'@'localhost'
FLUSH PRIVILEGES;


-- explicación de cada permiso
-- el usuario tiene permisos para mostrar tablas(select), añadir contenido(insert), actualizar contenido (update) y borrar datos propios(delete)

-- el admin tiene los mismos que el usuario pero se añaden mas como la creación(create), la modificación(alter), la eliminación (drop),creación o eliminación de índices(index), bloquear tablas para evitar cambios malos en el funcionamiento(lock tables), añadir una respuesta automática a cierto evento(trigger), creación de funciones y procedimientos y almacenados (create routine), modificar lo antes dicho(alter routine), ejecutarlas(execute)

-- grant option permite que los administradores de la cooperativa puedan asignar permisos que ya tienen ellos mismos a usuarios normales con el fin de añadir un nuevo administrador si se quiere o necesita
